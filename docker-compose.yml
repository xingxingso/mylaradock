version: '3'

networks:
  frontend:
    driver: ${NETWORKS_DRIVER}
  backend:
    driver: ${NETWORKS_DRIVER}

volumes:
  mysql:
    driver: ${VOLUMES_DRIVER}
  memcached:
    driver: ${VOLUMES_DRIVER}
  redis:
    driver: ${VOLUMES_DRIVER}
  elasticsearch:
    driver: ${VOLUMES_DRIVER}

services:

### NGINX Server #########################################
    nginx:
      build:
        context: ./images/nginx
        args:
          - CHANGE_SOURCE=${CHANGE_SOURCE}
      volumes:
        - ${APP_CODE_PATH_HOST}:${APP_CODE_PATH_CONTAINER}${APP_CODE_CONTAINER_FLAG}
        - ${NGINX_HOST_LOG_PATH}:/var/log/nginx
        - ${NGINX_SITES_PATH}:/etc/nginx/sites-available
        - ${NGINX_UPSTREAM_CONF_PATH}:/etc/nginx/conf.d/upstream.conf
      ports:
        - "${NGINX_HOST_HTTP_PORT}:80"
        - "${NGINX_HOST_HTTPS_PORT}:443"
      networks:
        - frontend
        - backend

### PHP-FPM ##############################################
    php-fpm-1:
      build:
        context: ./images/php-fpm
        args:
          - LARADOCK_PHP_VERSION=${PHP_VERSION_1}
          - ZZ_DOCKER_CONF_PATH=${ZZ_DOCKER_CONF_PATH_1}
      volumes:
        - ./config/php-fpm/php${PHP_VERSION_1}.ini:/usr/local/etc/php/php.ini
        - ${ZZ_DOCKER_CONF_PATH_1}:/usr/local/etc/php-fpm.d/zz-docker.conf
        - ${APP_CODE_PATH_HOST}:${APP_CODE_PATH_CONTAINER}${APP_CODE_CONTAINER_FLAG}
      expose:
        - "9001"
      extra_hosts:
        - "dockerhost:${DOCKER_HOST_IP}"
      networks:
        - backend

    php-fpm-2:
      build:
        context: ./images/php-fpm
        args:
          - LARADOCK_PHP_VERSION=${PHP_VERSION_2}
      volumes:
        - ./config/php-fpm/php${PHP_VERSION_2}.ini:/usr/local/etc/php/php.ini
        - ${ZZ_DOCKER_CONF_PATH_2}:/usr/local/etc/php-fpm.d/zz-docker.conf
        - ${APP_CODE_PATH_HOST}:${APP_CODE_PATH_CONTAINER}${APP_CODE_CONTAINER_FLAG}
      expose:
        - "9002"
      extra_hosts:
        - "dockerhost:${DOCKER_HOST_IP}"
      networks:
        - backend
